# server
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9

# analysis (analyze.py)
librosa>=0.10,<1.0
numpy>=1.26

# rendering (render.py) — chromium itself comes from `playwright install`
playwright>=1.44
